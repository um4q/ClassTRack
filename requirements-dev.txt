# Dev/test-only dependencies - not needed to run the app itself (see
# requirements.txt for that). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-qt>=4.4
